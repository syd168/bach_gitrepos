# 安全说明

## 负责任披露

若你发现**可利用的安全漏洞**（例如会话固定、令牌泄露路径、可未授权访问他人数据等），请**不要**在公开 Issue 中披露细节或贴出可利用的 exploit。

建议做法：

1. 使用 GitHub 的 **Security advisories**（若仓库已开启），或  
2. 通过仓库维护者在 GitHub 个人主页公开的联系方式（若有）**私信**说明概要，再按协商方式提供细节。

## 不属于安全漏洞的常见情况

以下一般按普通 Issue 处理即可：

- 用户自行在浏览器或日志中泄露 Personal Access Token（属于使用不当）。
- 在**未使用 HTTPS、未限制访问来源**的环境下部署本应用导致的风险（部署责任在用户侧；README 已提示使用 HTTPS 与访问控制）。

## 版本与修复

维护者会在确认问题后尽快评估修复，并在合理范围内通过 Release 或提交说明告知。
